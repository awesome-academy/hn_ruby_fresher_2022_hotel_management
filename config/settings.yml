user:
  email:
    max_length: 255
    email_regex: !ruby/regexp '/\A[\w+\-.]+@[a-z\d\-.]+\.[a-z]+\z/i'
  password:
    min_length: 6
  name:
    max_length: 50
admin:
  room:
    pagy:
      max_length: 10
